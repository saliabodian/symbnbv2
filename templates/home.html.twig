{% extends 'base.html.twig' %}

{% block title %}
    Bienvenu sur mon blog !
{% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron mt-3">
            <h1 class="display-3">Bienvenu sur SymBnB!</h1>
            <p class="lead">Nous mettons en relations les voyageurs et les propriétaires qui veulents profiter d'opportunités et gagner de l'argent en proposant leurs biens en location !</p>
            <hr class="my-4">
            <p>Vous êtes voyageur ? Et vous voulez voir oú vous pouurez loger lors de votre prochain déplacement ?</p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="#" role="button">Voir nos annonces</a>
            </p>
        </div>

        <h2 class="h1 my-5">Nos apparetements stars</h2>
        <div class="row">
            {% for data in ads %}
                {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
            {% endfor %}
        </div>
        <h2 class="h1 my-5">Nos propriétaires stars</h2>
        <div class="row">
            {% for data in users %}
                <div class="col">
                    <div class="card border-secondary mb-3">
                        <div class="card-body">
                            <h4 class="card-title">{{data.users.fullname}}</h4>

                            <hr>

                            <div class="card-text">
                                <img src="{{data.users.picture}}" alt="Avatar de {{data.users.fullname}}" class="float-left mr-3">
                                <p>
                                    {{data.users.introduction }}
                                </p>
                                <a href="{{path('user_show', {'slug': data.users.slug})}}" class="btn btn-primary float-right">En savoir plus !</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}